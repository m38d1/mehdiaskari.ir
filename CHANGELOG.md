# تغییرات mehdiaskari.ir

قالب: جدیدترین نسخه در بالا. هر نسخه = یک بخش کامل (تغییرات کاربری، کد، تست، «چه چیزی عوض نشد»).
شناسهٔ نسخه‌ها از شمارهٔ کش سرویس‌ورکر (`sw.js`) گرفته می‌شود.

---

## v45 — ۱۴۰۵-۰۶-۳۱ (پست جدید: پنج چک سلامت برنامه)

### تغییرات کاربری
- **پست جدید** «پنج چک سلامت برنامه که قبل از هر به‌روزرسانی باید بگیرید»: سرهای باز، لگ منفی، قید سخت، شناوری/مدت مشکوک و کار مانده در گذشته — با مسیر فیلتر در MSP و P6، تب دوزبانه، آکاردئون FAQ و کراس‌لینک به پست‌های MSP و P6.

### تغییرات کد
1. `blog/schedule-health-checks/index.html` جدید (از تمپلیت + متن کامل).
2. `posts.json`: ورودی جدید (fa/en) در صدر + کارت `blog/all` + `sitemap.xml` + بازتولید `blog/rss.xml`.
3. `cover.js`: `__SLUG_META` برای اسلاگ جدید (کاور گانت فیروزه‌ای، کلمه «کنترل پروژه»).
4. `sw.js`: `mehdiaskari-v44` → `mehdiaskari-v45` (posts.json کش‌فرست است).

### تست
`sync --check`، `check-html`، `check-posts 11/11`، `schema ok`، `rss --check`، `sw ok`، `node --check`.

### چه چیزی عوض نشد
اسلاگ‌ها و تاریخ پست‌های قبلی؛ منطق موتورهای lab.

---

## v44 — ۱۴۰۵-۰۶-۳۱ (کاور معنایی + پست‌های مرتبط + چرخش تخصص + تیک toast + پولیش تم روشن)

### تغییرات کاربری
- **کاور مش + حرف عنوان** (`cover.js` v5): به‌جای نمودارهای خشک — واش گرادیانی نرم بر اساس خانواده رنگ تگ + حرف اول عنوان به‌عنوان واترمارک + بافت نقطه‌ای؛ `data-tags`/`data-title` هم override می‌دهند.
- **پست‌های مرتبط** ته هر مقاله (۳ تای هم‌تگی، دوزبانه).
- **چرخش تخصص** در هیرو (هر ۳ ثانیه با محو، دوزبانه).
- **تیک موفقیت** متحرک داخل toast کپی.
- **تم روشن**: کم‌رنگ `#8892A0` ← `#5F6B7A` برای خوانایی (index، بلاگ، 404).

### تغییرات کد
1. `cover.js` v5: `__SLUG_META` + `__HUE_FAMILY` + `hueFor()`/`titleFor()`؛ مش + حرف عنوان؛ `__fillCovers` از `data-tags`/`data-title` می‌خواند.
2. `components.js`: `initRelated()` + ثبت در `initAll`.
3. `blog.css`: `.related-sec`.
4. `index.html`: `.hero-rotator` + IIFE چرخش؛ `.check` در toast؛ `year-fa` جدا از فوتر؛ همه رشته‌های جدید دوزبانه.
5. `sw.js`: `mehdiaskari-v43` → `mehdiaskari-v44`.

### تست
`check-html 21/21`، `check-posts 10/10`، `schema ok`، `sw ok`، `node --check`، مپینگ هر ۱۰ اسلاگ با node تأیید شد.

### چه چیزی عوض نشد
TOC/زمان مطالعه/نوار پیشرفت (از قبل بودند)؛ `posts.json` و اسلاگ‌ها.

---

## تعمیرات CI — ۱۴۰۵-۰۶-۲۳ (بلافاصله پس از v43، نسخهٔ کاربری نیست)

### مشکل
جاب `checks` در GitHub Actions از قبل (حداقل از `bcf4ccc` و پیش از v42/v43) قرمز بود. علت: خروجی‌های تولیدشده با `tools/sync-post-ui.py` و `tools/make-rss.py` روی پست mikrotik و `blog/rss.xml` ساخته شده بودند اما کامیت نشده بودند؛ ضمناً `dateModified` آن پست (`2026-09-07`) با تاریخ آخرین کامیت فایل (۱۴ سپتامبر = ۲۳ شهریور ۱۴۰۵) هم‌خوانی نداشت — چون `--check` مقدار `dateModified` را از تاریخ کامیتِ خودِ فایل انتظار دارد، تا این خط درست نشود جاب هر روز قرمز می‌ماند.

### انجام‌شده
- کامیت `4853d47`: `wordCount` اصلاح‌شدهٔ mikrotik (484 → 2282) + `blog/rss.xml` بازتولیدشده.
- کامیت بعدی: `dateModified: 2026-09-14` (خروجی خودِ tool) — پس از آن هر دو `--check` سبز و **پایدار** می‌مانند (تاریخ کامیت فایل دیگر تغییر نمی‌کند).

### تست
بازتولید کامل گام‌های CI روی درخت محلی: sync --check، rss --check، check-html، check-posts، check-schema، check-sw-version، node --check، دو موتور (101+71)، کارت ترمینال — همه exit 0. تأیید سبز شدن `checks` روی GitHub پس از پوش.

### چه چیزی عوض نشد
هیچ فایل CSS/JS/HTML رابطی دست نخورد؛ کش سرویس‌ورکر و شمارهٔ نسخه (v43) بدون تغییر — mikrotik و rss در مجموعهٔ تحت‌مراقبت `PRECACHE` نیستند.

---

## v43 — ۱۴۰۵-۰۶-۲۳ (مجموعهٔ ۱: ساعت تهران + تیلت کارت‌های وبلاگ + ارتقاء مغناطیسی)

### تغییرات کاربری
- **ساعت زندهٔ تهران** در هیرو، کنار دکمهٔ دانلود رزومه: پیل شیشه‌ای با نقطهٔ تپندهٔ فیروزه‌ای و زمان `HH:MM:SS` با فونت مونواسپیس و اعداد LTR؛ برچسب دوزبانه (تهران/Tehran) با سوییچ زبان هماهنگ می‌شود. در حالت reduced-motion فقط نقطه ثابت می‌شود (ساعت اطلاعاتی است، حذف نمی‌شود). در تب پنهان تیک‌تاک متوقف می‌شود (باتری).
- **تیلت سه‌بعدی روی کارت‌های وبلاگ**: کارت‌های خانه (`.blog-card`) و کارت‌های `/blog/all/` (`.post-card`) مثل کارت‌های ابزار/اطلاعات هیرو، ±۳.۵ درجه با موس می‌چرخند؛ lift هاور (-4px / -3px) حفظ شد؛ فقط `pointer:fine` و motion-safe.
- **مغناطیسی‌شدن دکمهٔ رزومه ارتقاء یافت**: به‌جای نوشتن مستقیم transform در هر mousemove، حالا rAF-throttle شده (قبلاً در موس‌های پرسرعت پرش داشت).
- **نکتهٔ صادقانه**: دو مورد از «مجموعهٔ ۱» که پیشنهاد شده بود — **نوار پیشرفت مطالعه + فهرست مطالب + زمان مطالعه** (در `components.js`) و **دکمهٔ مغناطیسی** — از قبل موجود بودند و بازسازی نشد؛ فقط مغناطیسی صیقل خورد.

### تغییرات کد
1. `index.html`: مارک‌آپ `.hero-clock` در `.hero-actions`؛ CSS بلوک ساعت؛ IIFE تیکر ساعت (Intl + fallback دستی UTC+3:30) قبل از `setLang('fa')`.
2. `index.html`: بازنویسی هندلر مغناطیسی با rAF guard.
3. `components.js`: تابع `initTilt(root)` + فراخوانی در `initAll`؛ idempotent با فلگ `__tiltBound`؛ ریشه‌ی self-match برای کارت‌های تزریق‌شده با MutationObserver.
4. `components.css`: بلوک `.blog-card.tiltable` / `.post-card.tiltable` (اسپسیفییتی بالاتر از هاورهای اصلی) + گارد reduced-motion.
5. `sw.js`: `mehdiaskari-v42` → `mehdiaskari-v43`.
6. `.gitignore`: افزودن `backup/` (پیش از این فایل‌های بکاپ منتشرشدنی بودند).

### تست‌ها
- `tools/check-html.py`: ۲۱/۲۱ متعادل.
- `node --check`: components.js / sw.js / cover.js + پارس هر دو اسکریپت اینلاین index سالم.
- `tools/test-wf-engine.js`: ۱۰۱ / `test-evm-engine.js`: ۷۱ سبز؛ رشتهٔ ادعای `101 W.F + 71 EVM assertions passed` دست‌نخورده.
- `tools/check-sw-version.py`: ok (تغییر cache-sensitive + به‌روزرسانی sw).
- دود jsdom (۱۲ بررسی): تیک ساعت، opt-in تیلت روی هر دو نوع کارت، ریست در pointerleave، idempotency، گارد reduced-motion.

### این نسخه چه چیزی را عوض نکرد
- محتوای پست‌ها، داده‌ها (`posts.json`)، رفرنس‌های سئو، تم/توکن‌ها، ناوبری، cmdk، jina.
- فایل‌های آلودهٔ `blog/mikrotik-load-balancing-filters/index.html` و `blog/rss.xml` عمداً بیرون از این کامیت ماندند.
- `components.js` برای نوار مطالعه/TOC/زمان مطالعه دست‌نخورد — از قبل کار می‌کرد.

---

## v42 — ۱۴۰۵-۰۶-۲۳ (پنج افکت بصری هیرو)

### تغییرات کاربری
- دی‌کد/اسکرمبل زیرعنوان هیرو (چرخش فعالیت‌ها)، تیلتر سه‌بعدی کارت‌های ابزار/اطلاعات، پاپ نقطهٔ تایم‌لاین، افکت ripple کلیک، و fallback دیسکِ توسعه‌یافته برای سوییچ تم در فایرفاکس (بدون View Transitions).

### تغییرات کد
1. `index.html`: بلوک VISUAL LAYER (~۴۸ خط CSS) + سه IIFE (tilt، dotPop بصری، ripple) + fallback `theme-disc`.
2. `sw.js`: `mehdiaskari-v41` → `v42`.

### تست‌ها
- دود jsdom ۱۳/۱۳ + ست کامل چک‌های CI (check-html ۲۱/۲۱، ۱۰۱+۷۱ تست موتور).

### این نسخه چه چیزی را عوض نکرد
- هیچ فایل محتوایی، داده یا ساختار صفحه‌ای تغییر نکرد؛ فقط `index.html` و `sw.js`.
